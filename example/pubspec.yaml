name: media_metadata_example
description: "Demonstrates how to use the media_metadata plugin."
publish_to: 'none'

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter

  media_metadata:
    path: ../
  file_picker: ^12.0.0-beta.5

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true